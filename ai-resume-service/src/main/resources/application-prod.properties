# 应用基本配置
spring.application.name=ai-resume-service
server.port=${SERVER_PORT:8080}

# 数据库配置 - 使用环境变量
spring.datasource.url=${DB_URL:jdbc:mysql://10.22.101.154:3306/ai_resume?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Gsc-6600326}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 数据库连接池配置
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.connection-test-query=SELECT 1

# JPA配置
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL57Dialect
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Shanghai

# Redis配置已移除
# spring.redis.host=${REDIS_HOST:localhost}
# spring.redis.port=${REDIS_PORT:6379}
# spring.redis.password=${REDIS_PASSWORD:}
# spring.redis.database=${REDIS_DATABASE:0}

# 文件上传配置
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# 微信小程序配置 - 使用环境变量
wechat.app-id=${WECHAT_APPID:wxd90100eb4eda3c1a}
wechat.app-secret=${WECHAT_APPSECRET:a2353a4d949c437b6f2ca32a1decc31a}

# AI API配置 - 使用环境变量
openai.api-key=${OPENAI_API_KEY:your_openai_api_key}
tongyi.api-key=${TONGYI_API_KEY:your_tongyi_api_key}
wenxin.api-key=${WENXIN_API_KEY:your_wenxin_api_key}

# 云存储配置 - 使用环境变量
oss.endpoint=${OSS_ENDPOINT:your_oss_endpoint}
oss.access-key=${OSS_ACCESS_KEY:your_oss_access_key}
oss.secret-key=${OSS_SECRET_KEY:your_oss_secret_key}
oss.bucket-name=${OSS_BUCKET_NAME:your_bucket_name}

# 日志配置

# DeepSeek API配置
# 注意：API密钥通过环境变量读取，不直接硬编码在配置文件中
deepseek.api.key=${DEEPSEEK_API_KEY}
deepseek.api.url=https://api.deepseek.com/v1

# 其他第三方API配置（如需）
logging.level.root=INFO
logging.level.com.aicv=INFO

# 跨域配置
cors.allowed-origins=*
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*

# 业务配置
app.free-optimize-count=2
app.max-batch-optimize=10
