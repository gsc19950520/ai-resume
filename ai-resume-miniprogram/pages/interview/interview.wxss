/* interview.wxss */
/* 简历选择弹窗样式 */
.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.5);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 9999;
}

.resume-select-modal {
  width: 80%;
  max-width: 600rpx;
  background-color: white;
  border-radius: 20rpx;
  overflow: hidden;
  box-shadow: 0 10rpx 40rpx rgba(0, 0, 0, 0.15);
}

.modal-header {
  padding: 40rpx;
  border-bottom: 1rpx solid #e0e0e0;
  text-align: center;
}

.modal-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #333;
}

.modal-content {
  padding: 30rpx;
  max-height: 600rpx;
  overflow-y: auto;
}

.resume-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.resume-item {
  padding: 30rpx;
  border: 2rpx solid #e0e0e0;
  border-radius: 12rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
  transition: all 0.3s ease;
}

.resume-item:hover {
  border-color: #667eea;
  background-color: #f8f9ff;
}

.resume-item.selected {
  border-color: #667eea;
  background-color: #f8f9ff;
}

.resume-info {
  flex: 1;
}

.resume-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
  margin-bottom: 8rpx;
}

.resume-occupation {
  font-size: 28rpx;
  color: #666;
  margin-bottom: 6rpx;
}

.resume-time {
  font-size: 24rpx;
  color: #999;
}

.resume-select-icon {
  width: 40rpx;
  height: 40rpx;
}

.resume-select-icon image {
  width: 100%;
  height: 100%;
}

.modal-footer {
  padding: 30rpx 40rpx;
  border-top: 1rpx solid #e0e0e0;
  text-align: center;
}

.tips {
  font-size: 24rpx;
  color: #999;
}

/* 全局样式重置和基础设置 */
page, view, text, image, button, input, textarea, navigator {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

page {
  font-family: "Inter", system-ui, sans-serif;
  background-color: #F8FAFC;
  color: #0F172A;
}

.container {
  padding: 20rpx;
  display: flex;
  flex-direction: column;
  min-height: 100vh;
  background-color: #F8FAFC;
}

/* 顶部导航栏 */
.header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 100rpx;
  background-color: #FFFFFF;
  padding: 0 30rpx;
  box-shadow: 0 2rpx 10rpx rgba(0, 0, 0, 0.1);
  border-radius: 0 0 14rpx 14rpx;
  margin-bottom: 20rpx;
}

.header-left {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.avatar-icon {
  width: 60rpx;
  height: 60rpx;
  border-radius: 50%;
}

.job-badge {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  font-size: 24rpx;
  padding: 8rpx 20rpx;
  border-radius: 30rpx;
  font-weight: 500;
}

.header-center {
  font-size: 36rpx;
  font-weight: bold;
  color: #0F172A;
  text-align: center;
}

.header-right {
  display: flex;
  align-items: center;
}

/* 进度区域 */
.progress-area {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
}

.countdown-timer {
  font-size: 28rpx;
  font-weight: 600;
  color: #4F46E5;
}

.countdown-timer.time-warning {
  color: #EF4444;
}

.progress-bar {
  width: 150rpx;
  height: 8rpx;
  background-color: #E2E8F0;
  border-radius: 4rpx;
  overflow: hidden;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #4F46E5 0%, #818CF8 100%);
  border-radius: 4rpx;
  transition: width 1s ease;
}

/* 面试官风格选择模块 */
.persona-selector {
  width: 100%;
  margin-bottom: 40rpx;
  animation: fade-in 0.5s ease-in;
  background-color: #FFFFFF;
  padding: 32rpx;
  border-radius: 16rpx;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
}

.selector-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #0F172A;
  margin-bottom: 20rpx;
  text-align: center;
  padding-bottom: 20rpx;
  border-bottom: 2rpx solid #E2E8F0;
}

/* 水平卡片布局 */
.persona-cards {
  display: flex;
  gap: 24rpx;
  overflow-x: auto;
  padding: 20rpx 0;
  -webkit-overflow-scrolling: touch;
  margin-bottom: 20rpx;
}

/* 隐藏滚动条但保持功能 */
.persona-cards::-webkit-scrollbar {
  display: none;
}

.persona-cards {
  -ms-overflow-style: none;
  scrollbar-width: none;
}

/* 卡片样式 */
.persona-card {
  width: 240rpx;
  height: 140rpx;
  border-radius: 16rpx;
  padding: 24rpx 20rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 16rpx;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  flex-shrink: 0;
  position: relative;
  overflow: hidden;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.05);
}

/* 添加卡片hover效果 */
.persona-card:hover {
  transform: translateY(-4rpx);
  box-shadow: 0 6rpx 20rpx rgba(0, 0, 0, 0.1);
}

/* 不同风格的背景色 */
.persona-card.persona-colloquial {
  background-color: #DCECFB;
}

.persona-card.persona-formal {
  background-color: #F3F0FF;
}

.persona-card.persona-manager {
  background-color: #FEF3C7;
}

.persona-card.persona-analytical {
  background-color: #E0F2FE;
}

.persona-card.persona-encouraging {
  background-color: #E8FDF5;
}

.persona-card.persona-pressure {
  background-color: #FFE4E6;
}

.persona-card:active {
  transform: scale(0.98);
}

.persona-card.selected {
  box-shadow: 0 0 0 6rpx #4F46E5, 0 6rpx 16rpx rgba(79, 70, 229, 0.2);
  transform: scale(1.05);
}

.persona-emoji {
  font-size: 68rpx;
  transition: transform 0.3s ease;
}

.persona-card.selected .persona-emoji {
  transform: scale(1.2);
}

.persona-name {
  font-size: 28rpx;
  font-weight: 600;
  color: #0F172A;
  text-align: center;
  line-height: 1.3;
}

/* 卡片选中效果增强 */
.persona-card.selected {
  box-shadow: 0 0 0 8rpx #4F46E5, 0 10rpx 24rpx rgba(79, 70, 229, 0.3);
  transform: scale(1.08) translateY(-4rpx);
  z-index: 10;
}

/* 预览区域 */
.preview-section {
  background: linear-gradient(135deg, #F7FAFF 0%, #EFF6FF 100%);
  border-radius: 16rpx;
  padding: 28rpx;
  margin: 20rpx 0;
  border-left: 8rpx solid #4F46E5;
  position: relative;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.05);
  animation: fade-in-up 0.3s ease-out;
}

.preview-question {
  font-size: 28rpx;
  color: #1E293B;
  line-height: 1.6;
  font-weight: 500;
  position: relative;
  padding-left: 40rpx;
}

.preview-question::before {
  content: '💬';
  position: absolute;
  left: 0;
  top: 0;
  font-size: 32rpx;
}

/* 开始面试按钮 */
.start-interview-btn {
  background-color: #4F46E5;
  color: white;
  font-size: 32rpx;
  font-weight: bold;
  border-radius: 14rpx;
  padding: 24rpx 0;
  margin-bottom: 30rpx;
  transition: all 0.3s ease;
}

.start-interview-btn.active {
  background-color: #4F46E5;
}

.start-interview-btn.disabled {
  background-color: #E2E8F0;
  color: #64748B;
}

.start-interview-btn:active {
  opacity: 0.9;
  transform: scale(0.98);
}

/* 主内容区域 */
.main-content {
  display: flex;
  flex-direction: row;
  gap: 20rpx;
  flex: 1;
}

/* 左侧列 */
.left-column {
  flex: 0 0 60%;
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

/* 右侧列 */
.right-column {
  flex: 0 0 40%;
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

/* 问题卡片 */
.question-card-wrapper {
  margin-bottom: 20rpx;
}

.question-card {
  background: #FFFFFF;
  padding: 40rpx;
  border-radius: 14rpx;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
  position: relative;
}

.question-header {
  margin-bottom: 20rpx;
}

.persona-label {
  display: inline-block;
  padding: 8rpx 20rpx;
  border-radius: 8rpx;
  font-size: 24rpx;
  font-weight: 600;
  margin-bottom: 16rpx;
}

.persona-label.persona-colloquial {
  background-color: #DCECFB;
  color: #1E40AF;
}

.persona-label.persona-formal {
  background-color: #F3F0FF;
  color: #6B21A8;
}

.persona-label.persona-manager {
  background-color: #FEF3C7;
  color: #92400E;
}

.persona-label.persona-analytical {
  background-color: #E0F2FE;
  color: #0369A1;
}

.persona-label.persona-encouraging {
  background-color: #E8FDF5;
  color: #065F46;
}

.persona-label.persona-pressure {
  background-color: #FFE4E6;
  color: #B91C1C;
}

.question-content {
  font-size: 36rpx;
  font-weight: bold;
  line-height: 1.6;
  color: #0F172A;
  margin-bottom: 20rpx;
}

.hint-text {
  font-size: 24rpx;
  color: #64748B;
  line-height: 1.5;
  padding: 16rpx;
  background-color: #F8FAFC;
  border-radius: 8rpx;
  writing-mode: horizontal-tb; /* 明确设置为横向显示 */
  word-break: break-word; /* 确保文本正常换行 */
  width: 100%; /* 确保占满可用宽度 */
  box-sizing: border-box; /* 包含padding在宽度内 */
}

.interview-history-list {
  background-color: #FFFFFF;
  border-radius: 16rpx;
  padding: 32rpx;
  margin-bottom: 32rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.08);
  transition: all 0.3s ease;
}

.history-list-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 32rpx;
  padding-bottom: 24rpx;
  border-bottom: 2rpx solid #F0F0F0;
}

.history-list-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #0F172A;
}

.history-list-count {
  font-size: 24rpx;
  color: #64748B;
  background-color: #F5F5F5;
  padding: 8rpx 20rpx;
  border-radius: 20rpx;
}

.history-list-content {
  max-height: 600rpx;
  overflow-y: auto;
}

.history-list-content::-webkit-scrollbar {
  width: 10rpx;
}

.history-list-content::-webkit-scrollbar-track {
  background-color: #F1F1F1;
  border-radius: 5rpx;
}

.history-list-content::-webkit-scrollbar-thumb {
  background-color: #C1C1C1;
  border-radius: 5rpx;
}

.history-list-content::-webkit-scrollbar-thumb:hover {
  background-color: #A8A8A8;
}

.history-list-item {
  margin-bottom: 36rpx;
  padding-bottom: 36rpx;
  border-bottom: 1rpx solid #F5F5F5;
}

.history-list-item:last-child {
  border-bottom: none;
  margin-bottom: 0;
  padding-bottom: 0;
}

.question-block {
  margin-bottom: 24rpx;
}

.answer-block {
  margin-top: 24rpx;
}

.block-header {
  display: flex;
  align-items: center;
  margin-bottom: 16rpx;
}

.block-icon {
  width: 60rpx;
  height: 60rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 16rpx;
  background-color: #F5F5F5;
  font-size: 28rpx;
}

.block-icon image {
  width: 50rpx;
  height: 50rpx;
  border-radius: 50%;
}

.question-icon {
  background: linear-gradient(135deg, #4F46E5, #818CF8);
  color: white;
}

.answer-icon {
  background: linear-gradient(135deg, #10B981, #34D399);
  color: white;
}

.block-title {
  font-weight: 600;
  color: #0F172A;
  flex: 1;
  font-size: 28rpx;
}

.block-time {
  font-size: 20rpx;
  color: #94A3B8;
}

.question-content {
  background-color: #F0F7FF;
  border-left: 8rpx solid #4F46E5;
  padding: 24rpx;
  border-radius: 0 12rpx 12rpx 0;
  font-size: 26rpx;
  line-height: 1.6;
  color: #0F172A;
  margin-left: 76rpx;
}

.answer-content {
  background-color: #F6FFF0;
  border-left: 8rpx solid #10B981;
  padding: 24rpx;
  border-radius: 0 12rpx 12rpx 0;
  font-size: 26rpx;
  line-height: 1.6;
  color: #0F172A;
  margin-left: 76rpx;
}

.fade-in {
  animation: fadeIn 0.5s ease-in;
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(16rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* 用户回答面板 */
.user-answer-panel {
  background-color: #FFFFFF;
  padding: 32rpx;
  border-radius: 14rpx;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
}

.panel-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24rpx;
}

.panel-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #0F172A;
}

.recording-status {
  font-size: 24rpx;
  color: #EF4444;
  font-weight: 500;
  animation: pulse 1s infinite;
}

/* 录音按钮 */
.record-button-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin: 30rpx 0;
}

.record-button {
  width: 112rpx;
  height: 112rpx;
  border-radius: 50%;
  background-color: #4F46E5;
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4rpx 12rpx rgba(79, 70, 229, 0.3);
  transition: all 0.3s ease;
}

.record-button.recording {
  background-color: #EF4444;
  box-shadow: 0 4rpx 12rpx rgba(239, 68, 68, 0.3);
}

.record-button:active {
  transform: scale(0.95);
}

.mic-icon {
  width: 48rpx;
  height: 48rpx;
}

.record-hint {
  margin-top: 16rpx;
  font-size: 24rpx;
  color: #64748B;
}

/* 转写文本框 */
.transcription-box {
  margin: 20rpx 0;
}

/* 语音识别状态指示器 */
.recognition-status-indicator {
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 15rpx;
  padding: 10rpx;
  background-color: #f0f9ff;
  border-radius: 8rpx;
}

.status-dots {
  display: flex;
  margin-right: 10rpx;
}

.dot {
  width: 12rpx;
  height: 12rpx;
  border-radius: 50%;
  background-color: #4F46E5;
  margin: 0 4rpx;
  animation: pulse 1.4s infinite ease-in-out both;
}

.dot-1 {
  animation-delay: -0.32s;
}

.dot-2 {
  animation-delay: -0.16s;
}

@keyframes pulse {
  0%, 80%, 100% {
    transform: scale(0);
  }
  40% {
    transform: scale(1.0);
  }
}

.status-text {
  font-size: 26rpx;
  color: #4F46E5;
  font-weight: 500;
}

/* 转写提示文本 */
.transcription-tips {
  margin-top: 15rpx;
  text-align: center;
}

.transcription-tips text {
  font-size: 24rpx;
  color: #94A3B8;
}

.transcription-textarea {
  width: 100%;
  min-height: 200rpx;
  padding: 24rpx;
  font-size: 28rpx;
  color: #0F172A;
  background-color: #F7FAFF;
  border: 2rpx solid #E0E7FF;
  border-radius: 12rpx;
  box-sizing: border-box;
}

/* 提交行 */
.submit-row {
  display: flex;
  gap: 16rpx;
  margin-top: 20rpx;
}

.submit-btn-primary {
  flex: 2;
  background-color: #4F46E5;
  color: white;
  font-size: 28rpx;
  font-weight: 600;
  border-radius: 12rpx;
  padding: 20rpx;
  transition: all 0.3s ease;
}

.submit-btn-secondary {
  flex: 1;
  background-color: #F1F5F9;
  color: #64748B;
  font-size: 28rpx;
  font-weight: 500;
  border-radius: 12rpx;
  padding: 20rpx;
  transition: all 0.3s ease;
}

.submit-btn-primary:active,
.submit-btn-secondary:active {
  opacity: 0.9;
  transform: scale(0.98);
}

/* AI反馈卡片 */
.ai-feedback-card {
  background-color: #FFFFFF;
  padding: 24rpx;
  border-radius: 14rpx;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
}

.feedback-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24rpx;
}

.feedback-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #0F172A;
}

.overall-score {
  font-size: 36rpx;
  font-weight: bold;
  color: #4F46E5;
}

/* 评分圆环 */
.score-donut {
  display: flex;
  justify-content: space-around;
  margin-bottom: 24rpx;
}

.score-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
}

.score-circle {
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  font-weight: bold;
  color: white;
}

.score-circle.tech {
  background-color: #3B82F6;
}

.score-circle.logic {
  background-color: #10B981;
}

.score-circle.clarity {
  background-color: #F59E0B;
}

.score-circle.depth {
  background-color: #8B5CF6;
}

.score-label {
  font-size: 22rpx;
  color: #64748B;
}

/* 匹配要点标签 */
.matched-points-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-bottom: 20rpx;
}

.matched-point-tag {
  background-color: #D1FAE5;
  color: #065F46;
  font-size: 22rpx;
  padding: 8rpx 16rpx;
  border-radius: 8rpx;
  font-weight: 500;
}

/* 快速提示 */
.quick-tips {
  font-size: 24rpx;
  color: #0F172A;
  line-height: 1.5;
  padding: 16rpx;
  background-color: #F8FAFC;
  border-radius: 8rpx;
  margin-bottom: 20rpx;
  writing-mode: horizontal-tb; /* 明确设置为横向显示 */
  word-break: break-word; /* 确保文本正常换行 */
  width: 100%; /* 确保占满可用宽度 */
  box-sizing: border-box; /* 包含padding在宽度内 */
}

/* 薪资匹配卡片 */
.salary-match-card {
  background-color: #FEF3C7;
  border-radius: 12rpx;
  padding: 20rpx;
  margin-top: 16rpx;
  border: 1rpx solid #FBBF24;
}

.salary-title {
  display: flex;
  align-items: center;
  font-size: 28rpx;
  font-weight: 600;
  color: #92400E;
  margin-bottom: 16rpx;
}

.salary-icon {
  width: 32rpx;
  height: 32rpx;
  margin-right: 12rpx;
}

.salary-content {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.salary-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12rpx 0;
  border-bottom: 1rpx solid rgba(180, 83, 9, 0.2);
}

.salary-item:last-child {
  border-bottom: none;
}

.salary-label {
  font-size: 24rpx;
  color: #64748B;
}

.salary-value {
  font-size: 28rpx;
  font-weight: 600;
  color: #92400E;
}

.salary-match {
  color: #059669;
}

.match-status {
  font-size: 22rpx;
  font-weight: 400;
  margin-left: 8rpx;
  color: #64748B;
}

/* 底部控制按钮 */
.footer-controls {
  display: flex;
  gap: 20rpx;
  padding: 20rpx 0;
  background-color: #FFFFFF;
  border-radius: 14rpx 14rpx 0 0;
  box-shadow: 0 -2rpx 10rpx rgba(0, 0, 0, 0.05);
  margin-top: auto;
}

.footer-btn {
  flex: 1;
  font-size: 28rpx;
  font-weight: 600;
  padding: 24rpx 0;
  border-radius: 12rpx;
  transition: all 0.3s ease;
}

.footer-btn-primary {
  background-color: #4F46E5;
  color: white;
}

.footer-btn-secondary {
  background-color: #F1F5F9;
  color: #64748B;
}

.footer-btn-warning {
  background-color: #FEF3C7;
  color: #92400E;
}

.footer-btn:active {
  opacity: 0.9;
  transform: scale(0.98);
}

/* 时间警告覆盖层 */
.time-warning-overlay {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background-color: rgba(0, 0, 0, 0.8);
  padding: 40rpx;
  border-radius: 16rpx;
  z-index: 9999;
  animation: fade-in 0.3s ease;
}

.warning-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20rpx;
}

.warning-icon {
  font-size: 60rpx;
}

.warning-text {
  font-size: 32rpx;
  color: white;
  font-weight: 600;
}

.warning-time {
  font-size: 40rpx;
  color: #EF4444;
  font-weight: bold;
}

/* 动画效果 */
@keyframes fade-in {
  from {
    opacity: 0;
    transform: translateY(20rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes fade-in-up {
  from {
    opacity: 0;
    transform: translateY(20rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes fade-in-bottom {
  from {
    opacity: 0;
    transform: translateY(16rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes slide-down {
  from {
    opacity: 0;
    transform: translateY(-10rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes pulse {
  0% {
    opacity: 1;
  }
  50% {
    opacity: 0.6;
  }
  100% {
    opacity: 1;
  }
}

/* 响应式布局 */
@media screen and (max-width: 768px) {
  .main-content {
    flex-direction: column;
  }
  
  .left-column,
  .right-column {
    flex: 1;
    width: 100%;
  }
  
  .header-center {
    font-size: 32rpx;
  }
  
  .job-badge {
    font-size: 20rpx;
    padding: 6rpx 16rpx;
  }
  
  .question-content {
    font-size: 32rpx;
  }
}