<!--pages/report/report.wxml-->
<view class="report-container">

  <!-- 报告内容区域 -->
  <view class="report-content">
    <!-- 使用rich-text组件渲染markdown内容 -->
    <rich-text nodes="{{renderedContent}}" class="markdown-content"></rich-text>
  </view>

  <!-- 加载完成提示 -->
  <view class="complete-tip" wx:if="{{isComplete && !isLoading}}">
    <text class="complete-text">报告生成完成</text>
  </view>
  
  <!-- 保存报告按钮 -->
  <view class="report-actions" wx:if="{{isComplete && !isLoading && !isSaved}}">
    <button class="save-btn" bindtap="saveReport">
      保存报告
    </button>
  </view>
  
  <!-- 保存成功提示 -->
  <view class="save-success-tip" wx:if="{{isSaved}}">
    <text class="save-success-text">报告已保存</text>
  </view>
</view>