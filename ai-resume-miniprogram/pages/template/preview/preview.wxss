/* 样式隔离机制：通过容器类名隔离不同模板的样式 */
/* 各模板样式将通过各自的命名空间前缀实现隔离 */

/* 导入各模板样式 */
@import "./template/one/template-one.wxss";
@import "./template/two/template-two.wxss";
@import "./template/three/template-three.wxss";
@import "./template/four/template-four.wxss";
@import "./template/five/template-five.wxss";
@import "./template/six/template-six.wxss";

/* 基础样式设置，确保模板内容正常显示 */

/* 重置所有可能泄漏的页面级样式 */
page {
  background-color: #f5f5f5;
  padding: 0;
  overflow-y: auto;
  position: relative;
  z-index: 1;
}

/* 预览容器样式 - 添加背景色和更高的z-index以防止其他页面内容泄漏 */
.preview-container {
  width: 100%;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  box-sizing: border-box;
  background-color: #f5f5f5;
  position: relative;
  z-index: 2;
}

/* 动态模板渲染样式 - 确保内容不会被底部固定操作区域覆盖 */
.template-content {
  flex: 1;
  padding: 20rpx;
  padding-bottom: 650rpx; /* 增加底部内边距，确保内容不会被两个固定定位的操作区域遮挡 */
  overflow-y: auto;
  box-sizing: border-box;
  position: relative;
  z-index: 3;
  background-color: #f5f5f5;
  min-height: 0; /* 允许flex子元素在需要时缩小 */
}

/* 全局重置 - 防止list页面样式泄漏到预览页面 */
.section-title {
  /* 重置list.wxss中可能泄漏的.section-title样式 */
  all: unset;
}

/* 操作区域样式 - 包含模板切换和下载功能 */
.action-container {
  position: fixed;
  bottom: 0;
  left: 0;
  width: 100%;
  padding: 20rpx;
  background-color: #ffffff; /* 使用完全不透明的白色背景 */
  box-shadow: 0 -2rpx 10rpx rgba(0, 0, 0, 0.1);
  z-index: 9999;
  box-sizing: border-box;
  padding-bottom: 30rpx;
  margin: 0;
  /* 确保在安全区域内显示（适配刘海屏等设备） */
  padding-bottom: calc(30rpx + env(safe-area-inset-bottom));
}

/* 模板切换操作区域特定样式 - 确保完全覆盖下面的内容 */
.template-action-container {
  bottom: 180rpx; /* 与下载按钮区域保持合适距离 */
  padding-bottom: 20rpx;
  background-color: #ffffff; /* 使用完全不透明的白色背景 */
  /* 增加顶部阴影，使过渡更自然 */
  box-shadow: 0 -4rpx 12rpx rgba(0, 0, 0, 0.1);
}

/* 模板切换区域样式 - 确保完全覆盖内容 */
.template-switch-container {
  margin-bottom: 0;
  padding: 0;
}

.template-scroll {
  white-space: nowrap;
  width: 100%;
  padding-bottom: 5rpx;
}

.template-thumbnails {
  display: flex;
  width: 125%;
  padding-bottom: 10rpx;
  margin: 0;
}

/* 单个模板项样式 */
.template-item {
  display: inline-block;
  width: 280rpx;
  margin-right: 10rpx;
  text-align: center;
  position: relative;
  transition: transform 0.2s;
}

.template-item:active {
  transform: scale(0.95);
}

.template-item.active::after {
  content: '';
  position: absolute;
  bottom: -5rpx;
  left: 30rpx;
  right: 30rpx;
  height: 6rpx;
  background-color: #1677ff;
  border-radius: 3rpx;
}

/* 模板图片样式 */
.template-image {
  width: 100%;
  height: 200rpx;
  border-radius: 12rpx;
  border: 2rpx solid #e0e0e0;
  box-sizing: border-box;
}

.template-item.active .template-image {
  border-color: #1677ff;
  box-shadow: 0 0 0 2rpx rgba(22, 119, 255, 0.2);
}

.use-template-btn {
  width: 100%;
  background-color: #1677ff;
  color: white;
  border: none;
  padding: 20rpx 0;
  font-size: 32rpx;
  border-radius: 8rpx;
  box-shadow: 0 2rpx 8rpx rgba(22, 119, 255, 0.3);
  margin: 0;
  display: flex;
  align-items: center;
  justify-content: center;
}

.use-template-btn:active {
  background-color: #0958d9;
}

/* 按钮组样式 - 确保无间隙覆盖 */
.button-group {
  display: flex;
  gap: 20rpx;
  align-items: center;
  margin: 0;
  padding: 0;
}

/* 编辑简历按钮样式 */
.edit-resume-btn {
  flex: 1;
  background-color: #1677ff;
  color: white;
  border: none;
  padding: 24rpx 0;
  font-size: 30rpx;
  border-radius: 12rpx;
  box-shadow: 0 4rpx 12rpx rgba(22, 119, 255, 0.3);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 500;
  transition: all 0.2s ease;
}

.edit-resume-btn:active {
  background-color: #0958d9;
  transform: translateY(2rpx);
  box-shadow: 0 2rpx 6rpx rgba(22, 119, 255, 0.2);
}

/* 下载PDF按钮样式 */
.download-pdf-btn {
  flex: 1;
  background-color: #52c41a;
  color: white;
  border: none;
  padding: 24rpx 0;
  font-size: 30rpx;
  border-radius: 12rpx;
  box-shadow: 0 4rpx 12rpx rgba(82, 196, 26, 0.3);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 500;
  transition: all 0.2s ease;
}

.download-pdf-btn:active {
  background-color: #389e0d;
  transform: translateY(2rpx);
  box-shadow: 0 2rpx 6rpx rgba(82, 196, 26, 0.2);
}