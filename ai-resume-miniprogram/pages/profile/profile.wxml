<!--profile.wxml-->
<view class="profile-container">
  <!-- 用户信息区域 -->
  <view class="user-info-section" bindtap="onUserInfoTap">
    <view class="avatar">
      <image src="{{userInfo ? userInfo.avatarUrl : '/images/avatar.jpg'}}" mode="aspectFill"></image>
    </view>
    <view class="user-info">
      <view class="nickname">{{userInfo ? userInfo.nickName : '点击登录'}}</view>
      <view class="user-id">{{userInfo ? '' : '登录后享受更多功能'}}</view>
    </view>
    <view class="arrow">
      <image src="/images/arrow.png" mode="aspectFit"></image>
    </view>
  </view>

  <!-- 统计数据区域 -->
  <view class="stats-section" wx:if="{{userInfo}}">
    <view class="stat-item">
      <view class="stat-number">{{resumeCount || 0}}</view>
      <view class="stat-label">我的简历</view>
    </view>
    <view class="stat-divider"></view>
    <view class="stat-item">
      <view class="stat-number">{{interviewCount || 0}}</view>
      <view class="stat-label">面试次数</view>
    </view>
  </view>
  
  <!-- 功能列表 -->
  <view class="menu-section">
    <view class="menu-item" bindtap="editProfile">
      <view class="menu-icon">
        <image src="/images/user.png" mode="aspectFit"></image>
      </view>
      <view class="menu-text">信息编辑</view>
      <view class="menu-arrow">
        <image src="/images/arrow.png" mode="aspectFit"></image>
      </view>
    </view>
    <view class="menu-item" bindtap="interviewHistory">
      <view class="menu-icon">
        <image src="/images/history.png" mode="aspectFit"></image>
      </view>
      <view class="menu-text">面试历史</view>
      <view class="menu-arrow">
        <image src="/images/arrow.png" mode="aspectFit"></image>
      </view>
    </view>
    
    <view class="menu-item" bindtap="feedback">
      <view class="menu-icon">
        <image src="/images/feedback.png" mode="aspectFit"></image>
      </view>
      <view class="menu-text">意见反馈</view>
      <view class="menu-arrow">
        <image src="/images/arrow.png" mode="aspectFit"></image>
      </view>
    </view>
    
    <view class="menu-item" bindtap="about">
      <view class="menu-icon">
        <image src="/images/about.png" mode="aspectFit"></image>
      </view>
      <view class="menu-text">关于我们</view>
      <view class="menu-arrow">
        <image src="/images/arrow.png" mode="aspectFit"></image>
      </view>
    </view>
  </view>

  <!-- 退出登录按钮 -->
  <button class="logout-btn" wx:if="{{userInfo}}" bindtap="logout">退出登录</button>
  
  <!-- 版本信息 -->
  <view class="version-info">版本 1.0.0</view>

</view>